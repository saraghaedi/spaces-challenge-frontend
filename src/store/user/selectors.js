export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user;

export const selectUserSpace = (state) => state.user.space;

export const selectSpaceId = (state) => state.user.space.id;
